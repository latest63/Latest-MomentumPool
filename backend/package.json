{
  "name": "momentum-pool-backend",
  "version": "1.0.0",
  "scripts": {
    "relayer": "tsx src/relayer.ts",
    "server": "tsx src/server.ts"
  },
  "dependencies": {
    "viem": "^2.x",
    "dotenv": "^16.x",
    "express": "^4.x",
    "cors": "^2.x"
  },
  "devDependencies": {
    "tsx": "^4.x",
    "typescript": "^5.x",
    "@types/express": "^4.x",
    "@types/cors": "^2.x"
  }
}
